import 'package:flutter/material.dart';

class StylePaddings {
  static const double xxsValue = 4.0;
  static const double xsValue = 8.0;
  static const double sValue = 12.0;
  static const double mValue = 16.0;
  static const double lValue = 24.0;
  static const double xlValue = 32.0;

  static const EdgeInsets xxs = EdgeInsets.all(StylePaddings.xxsValue);
  static const EdgeInsets xs = EdgeInsets.all(StylePaddings.xsValue);
  static const EdgeInsets s = EdgeInsets.all(StylePaddings.sValue);
  static const EdgeInsets m = EdgeInsets.all(StylePaddings.mValue);
  static const EdgeInsets l = EdgeInsets.all(StylePaddings.lValue);
  static const EdgeInsets xl = EdgeInsets.all(StylePaddings.xlValue);
}
