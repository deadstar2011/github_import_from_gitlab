import 'package:flutter/material.dart';
import 'package:optima_delivery_mobile/common/styles/colors.dart';

class StyleShadows {
  static const shadow = BoxShadow(
    color: StyleColors.buttonShadowColor,
    offset: Offset(
      0.0,
      5.0,
    ),
    blurRadius: 15,
  );
}
